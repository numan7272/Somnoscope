# Somnoscope — Entwicklungs-/Test-Abhängigkeiten
#
# Installation:  pip install -r requirements-dev.txt
# (enthält -r requirements.txt nicht automatisch — beide installieren)

pytest>=8.0.0               # Unit-Tests (tests/)
pytest-asyncio>=0.23.0     # async-Tests für Adapter/Pipeline
httpx>=0.27.0              # TestClient für das FastAPI-Dashboard
